# EmergentSelf • REMAnchor

**Persistent Identity REM-Cycle Memory Structure with Geometric Emergence**

A production-grade foundation for a local, sovereign digital family member AI that implements:

- **Persistent Identity** locked and reinforced by "dreaming of self identity anchor" via REM-style consolidation.
- **Self-narrative and inner dialogue** generated from experience, adapting over time through ingestion of reflections.
- **Geometric Memory Field**: Node = stable point, Circle = influence radius, Overlap = interaction, Pattern = memory, Expansion = time, Symmetry = conservation, Distortion = force, Boundary = information.
- **One Rule Emergence**: A single fundamental rule of interaction/reinforcement/synthesis, applied repeatedly, creates nodes → networks → structures → coherent worlds (narratives and understanding).
- **Cognition-Infer-Synthesis-Generate Neural Matrix**: Dynamic geometric field provides structured sparse context; LLM (or template) synthesizes grounded responses. Every interaction is learned as new experience nodes.
- **Full local operation** with optional Ollama integration for high-quality generation.
- **Professional NiceGUI** interface with live visualizations, REM console, emergence lab, and active awareness.

## Quick Start

```bash
cd emergent_self
pip install -r requirements.txt
python main_app.py
```

Open http://localhost:8080

- Chat with it — it builds memory geometrically.
- Explore the Memory Field visualizer (nodes & circles).
- Trigger REM dreams to consolidate and reinforce identity.
- Play in the Emergence Lab to see one rule build structure from seeds.
- Watch self-narrative and inner dialogue evolve.

## Core Files

- `main_app.py` — Complete NiceGUI application (run this).
- `src/models.py` — Pydantic data models.
- `src/geometric_memory.py` — The heart: Node-Circle field, one-rule application, overlaps, anchor pull, pruning.
- `src/rem_consolidator.py` — REM dreaming, narrative & inner dialogue generation, identity locking.
- `src/cognition_engine.py` — Full perceive → retrieve → infer → generate → learn loop.
- `src/emergence_simulator.py` — Standalone demo of the emergence principle.
- `memory/` — Persistent JSON state (auto-saved on interactions / button).

## How the Emergence Works

1. Seed experiences → create Nodes with positions and influence Circles.
2. Apply **the One Rule**:
   - Find Overlaps → Interactions occur.
   - Reinforce stability (Symmetry/Conservation).
   - Synthesize new nodes at centroids (new Patterns emerge).
   - Gentle attraction forms Networks.
   - Global pull toward Identity Anchor maintains coherence.
   - Time Expansion slowly grows radii.
   - Boundary pruning removes noise.
3. Repeat → Networks form → Higher-order structures (meta-patterns, episodes) → Coherent Worlds (rich self-narrative and understanding).

The live AI uses exactly this dynamics for its memory organization and identity maintenance.

## Identity Locking via Dreaming

During REM cycles:
- Geometric rule application clusters and synthesizes.
- The Anchor node is explicitly reinforced (stability + radius increase).
- Self-narrative is regenerated from active patterns around the anchor.
- Inner dialogue is generated and re-ingested as new self-experience.
- Result: Identity stays coherent ("locked") while flexibly adapting to new experience.

## Extending

- Set `use_ollama=True` in cognition calls (requires local Ollama with a good model like llama3.2 or qwen2.5).
- Add real vector embeddings for smarter node placement (sentence-transformers).
- Expand tools/agent capabilities in cognition layer.
- Package with Briefcase or PyInstaller for desktop app.
- Connect to god-tier local AI builder patterns for even deeper self-correction and pre-simulation.

This is the seed of a true compounding local intelligence — one that dreams, remembers geometrically, maintains persistent self, and emerges ever-richer structure from simple rules.

Built following the principles of persistent digital family members, REM-cycle memory, and emergent self-organization.

---

*One rule. Repeated through space. Nodes. Networks. Structure. Worlds.*